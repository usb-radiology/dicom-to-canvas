{
  "name": "dicom-to-canvas",
  "version": "1.0.0",
  "description": "",
  "main": "dist/dicom-to-canvas",
  "scripts": {
    "dev": "tsc -w -p tsconfig.json",
    "dist": "rm -rf ./dist && tsc -p tsconfig.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Victor Parmar",
  "license": "ISC",
  "dependencies": {
    "canvas": "2.10.0",
    "dicom-parser": "1.8.13"
  },
  "peerDependencies": {
    "canvas": "2.10.0"
  },
  "devDependencies": {
    "@types/node": "^18.7.15",
    "typescript": "4.8.2"
  }
}
